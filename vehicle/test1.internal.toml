[timeouts]
#server = 30
driver = 5
mission = 5
remote_compute = 5
local_compute = 5

[services]
kernel = 'unix:///tmp/test1-kernel.sock'
driver = 'unix:///tmp/test1-driver.sock'
mission = 'unix:///tmp/test1-mission.sock'
flight_log = 'unix:///tmp/test1-log.sock'

[streams]
driver_telemetry = 'unix:///tmp/test1-driver_telem.sock'
mission_telemetry = 'unix:///tmp/test1-mission_telem.sock'
imagery = 'unix:///tmp/test1-imagery.sock'
# NOTE: Uncomment the local compute socket if you are
# using a local compute server.
#local_compute = 'unix:///tmp/test1-local_compute.sock'
results = 'unix:///tmp/test1-results.sock'
