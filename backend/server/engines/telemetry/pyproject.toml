[project]
name = "steeleagle-backend-telemetry"
version = "1.0.0"
description = "Proesses telemetry payloads from vehicles."
readme = "README.md"
requires-python = "==3.11.*"
dependencies = [
   "redis",
   "opencv-python-headless>=3, <5",
   "numpy",
   "pytz",
   "Pillow",
   "gabriel-server>=5.1.0",
   "steeleagle-protocol>=0.1.15",
   "foxglove-sdk",
]
