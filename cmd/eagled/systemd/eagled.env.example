# Sourced by eagled.service (EnvironmentFile=-/etc/eagled/eagled.env). Copy to
# /etc/eagled/eagled.env and edit as needed. install.sh writes this file on
# every run, not just the first -- so re-running it always overwrites
# whatever is here with TS_AUTHKEY/TS_VEHICLE_AUTHKEY from its own
# environment or prompts, letting you rotate the keys by re-running the
# installer. Manual edits to this file will not survive a re-run.

# Reusable Tailscale auth key eagled's own tsnet node joins with. This env var
# name is fixed, not configurable. Leave unset to fall back to tsnet's
# interactive login flow.
#TS_AUTHKEY=

# Optional separate auth key for vehicles' own tsnet nodes. This env var name
# is fixed too; leave unset to have vehicles reuse TS_AUTHKEY.
#TS_VEHICLE_AUTHKEY=
